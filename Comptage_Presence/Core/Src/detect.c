/*
 * detect.c
 *
 *  Created on: Feb 16, 2026
 *      Author: imane
 */

/*#include "detect.h"
#include "utils.h"

#define SEUIL 2000 // distance seuil en mm

void detect_zones(uint16_t tab[8][8], counter)
{
    if(moy(tab[4]) < SEUIL)
    {
    	while(moy(tab[8]) > SEUIL) {}
    	while(moy(tab[8]) < SEUIL) {}
    	counter++;
    }

    else if(moy(tab[5]) < SEUIL)
    {
    	while(moy(tab[1]) > SEUIL) {}
    	while(moy(tab[1]) < SEUIL) {}
    	counter--;
    }
}*/

